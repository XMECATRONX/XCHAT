import { X } from 'lucide-react'; export default function StoryViewer({ story, onClose }: any) { if (!story) return null; return <div className='fixed inset-0 bg-black z-50 flex flex-col'><div className='p-4 flex justify-between items-center bg-gradient-to-b from-black/60 to-transparent'><span className='text-[#00F5A0] font-bold'>{story.user}</span><button onClick={onClose}><X className='w-6 h-6' /></button></div><img src={story.content} className='flex-1 object-contain' /></div> }
